# K-fold-cross-validation
Implementation k-fold cross-validation, which can help us to obtain reliable estimates of the model’s generalisation performance, that is, how well the model performs on unseen data.
